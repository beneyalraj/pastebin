{
  "name": "pastebin-clone",
  "version": "1.0.0",
  "description": "A Pastebin-like service with Node.js, Express, Neon PostgreSQL, and Vercel.",
  "main": "index.js",
"scripts": {
  "start": "node index.js",
  "dev": "nodemon index.js",
  "build": "cd client && npm install && npm run build",
  "vercel-build": "npm run build"
},
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "nanoid": "^3.3.18",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.x"
  }
}
